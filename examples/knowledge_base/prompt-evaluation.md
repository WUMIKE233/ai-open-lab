# Prompt Evaluation

Prompt evaluation turns expected behavior into repeatable checks. A small suite can catch regressions when prompts, model settings, or retrieval context changes.

Useful checks include required keywords, forbidden phrases, structured output validation, and task-specific assertions.
