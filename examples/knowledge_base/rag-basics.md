# RAG Basics

Retrieval-augmented generation uses a retriever to find relevant context before asking a model to answer.

A simple RAG workflow loads documents, chunks text, ranks chunks for a query, and passes the best snippets into the final prompt.
